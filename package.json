{
  "name": "causa-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Causa attribution core — the deterministic engine that settles agent outcomes: join, causal ladder, verdicts",
  "scripts": {
    "reconcile": "tsx scripts/generate-ledger.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.17.9",
    "tsx": "^4.23.0",
    "typescript": "^5.7.2",
    "vitest": "^4.1.10"
  }
}
