{
  "name": "causa-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Causa attribution core — the deterministic engine that settles agent outcomes: join, causal ladder, verdicts",
  "scripts": {
    "reconcile": "tsx scripts/generate-ledger.ts",
    "settle": "tsx src/cli.ts settle",
    "preflight": "tsx src/cli.ts preflight",
    "example": "tsx scripts/export-meridian-example.ts && tsx src/cli.ts settle examples/meridian/engagement.json",
    "build:viewer": "tsx scripts/build-viewer.ts",
    "build:playground": "tsx scripts/build-playground.ts",
    "build:web": "npm run build:viewer && npm run build:playground",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.17.9",
    "esbuild": "^0.28.1",
    "tsx": "^4.23.0",
    "typescript": "^5.7.2",
    "vitest": "^4.1.10"
  }
}
